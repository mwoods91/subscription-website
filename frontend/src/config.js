export const drawerWidth = 240;

// ==============================|| THEME CONFIGURATION  ||============================== //

const config = {
  defaultPath: '/my-id-card',
  fontFamily: `'Public Sans', sans-serif`,
  miniDrawer: false,
  container: true,
  mode: 'light',
  presetColor: 'default',
  themeDirection: 'ltr'
};

export default config;
